<?php
  // 編輯轉靜態 讀取選擇，.html or .md
  $_format = '.md';

  // 輸出靜態頁面的格式
  $_oput_format = '.html';

  // list 一頁多少個預覽
  $_a_page_limit = 2;

  // 分頁是否顯示 下一頁
  $_is_show_next = true;

  // 分頁是否顯示 上一頁
  $_is_show_prev = true;

  // 分頁顯示數量，0 則不限制
  $_pagination_limit = 2;
